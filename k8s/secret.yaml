apiVersion: v1
kind: Secret
metadata:
  name: next-secret
type: Opaque
stringData:
  # Replace these placeholder values before applying in production
  NEXTAUTH_SECRET: "REPLACE_WITH_STRONG_SECRET"
  STRIPE_SECRET_KEY: "REPLACE_WITH_STRIPE_SECRET"
